# JsonSerializationExample

Bu basit C# konsol uygulamasý, `System.Text.Json` kullanarak bir `Person` nesnesinin nasýl JSON formatýna dönüþtürüleceðini (serialize) ve bir JSON string'inden nasýl nesneye dönüþtürüleceðini (deserialize) gösterir.

## Nasýl Çalýþtýrýlýr?

1. .NET SDK'nýn kurulu olduðundan emin ol (örn: .NET 6 veya üzeri).
2. Proje klasöründe terminal aç:
   
```bash
dotnet run
